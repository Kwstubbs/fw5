Run the separate Python fuzzer:

	python3.11 fuzzer.py

The fuzzer saves the first input that makes target.py exit unsuccessfully. Replay
the saved input by replacing the filename below with the reported filename:

	python3.11 target.py < crash-0.bin

Questions
---------

1. Why does most random input not crash?
2. Why does input containing two equals signs crash?
3. How could a fuzzer increase its chances of reaching the vulnerable code?
4. How could target.py split only on the first equals sign?
5. How could target.py reject malformed input without throwing an exception?